This is the RC_MAC-write-a-block package meant as a guide to building
out-of-tree packages. To use the RC_MAC blocks, the Python namespaces
is in 'RC_MAC', which is imported as:

    import RC_MAC

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(RC_MAC)
